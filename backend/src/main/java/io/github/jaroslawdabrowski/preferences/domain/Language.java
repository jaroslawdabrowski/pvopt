package io.github.jaroslawdabrowski.preferences.domain;

/**
 * UI language the frontend should render in. Kept in the domain as a plain enum -
 * translation content itself lives entirely in the frontend's i18n files.
 */
public enum Language {
    EN,
    PL
}
